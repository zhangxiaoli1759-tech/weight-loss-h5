// ============================================================
// Supabase 配置
// 方式一：直接在此填写（然后重新部署）
// 方式二：在页面右上角 ⚙ 设置里粘贴（存于浏览器 localStorage，无需改代码）
// 两者都为空时，自动使用浏览器本地存储(localStorage)，页面立即可用。
// ============================================================
window.WL_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
